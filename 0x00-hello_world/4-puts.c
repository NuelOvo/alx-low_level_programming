#include <stdio.h>

/**
 * main - This is the Entry point for the function
 *
 * puts: this is the output stream holding our value
 *
 * Return: 0 is the expected value, which is true, no error
 */

int main(void)
{
	puts("Programming is like building a multilingual puzzle\n");
	return (0);
}
